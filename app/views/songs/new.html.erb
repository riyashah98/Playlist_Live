<h2> Add a New Song to the Stream! </h2>

<%= form_tag create_song_path do %>
	<%= label_tag(:song_name, "Song Name: ") %>
	<%= text_field_tag(:song_name)%>
	<%= label_tag(:artist_name, "Artist Name: ") %>
	<%= text_field_tag(:artist_name) %>
	<%= submit_tag("Add Song!") %>
<% end %>